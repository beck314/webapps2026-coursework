from django.shortcuts import render

# Create your views here.

def index(request):
    if "SELECT level FROM userAccounts WHERE username == user" == admin:
        ##serve admin webpages
    else:
        ##denied